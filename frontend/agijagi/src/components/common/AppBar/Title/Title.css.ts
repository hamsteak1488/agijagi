import styled from '@emotion/styled';

export const Title = styled.div`
  position: absolute;
  left: 50%;
  font-size: 1.25rem;
  font-weight: 700;
  transform: translateX(-50%);
  pointer-events: none;
  user-select: none;
`;
